import { Text } from "react-native";

export default function Page() {
  return <Text>Checking rest syntax</Text>;
}

// so rest syntax
// Multiple slugs can be matched in a single route by using the rest syntax
// matches anything after the the parent url
// so in this case it will match anything after /blog so its like /blog*
